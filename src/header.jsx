import React from 'react'

const Header = React.memo(() => {
   return (
     <div className="header">
        <div className="Logo">
        Header Bruv
        </div>
      </div>);
})

export default Header;
